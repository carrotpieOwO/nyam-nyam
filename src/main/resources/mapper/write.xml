<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carrot.nyam.repository.ReviewRepository">


	<insert id="write">
		insert into review(image1, image2, image3, shopName, content, 
		rating, price, location, userId, createDate)
		values(#{image1}, #{image2}, #{image3}, #{shopName}, #{content}, 
		#{rating}, #{price}, #{location}, #{userId}, now())
	</insert>
	
</mapper>